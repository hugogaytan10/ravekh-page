import { catalogWebFeatureRoutes, catalogWebRoutesModel, getCatalogWebRoute } from "../../catalog-web";

export { catalogWebFeatureRoutes, catalogWebRoutesModel, getCatalogWebRoute };

export const catalogWebSystem = {
  key: "catalog-web",
  label: "Sistema de catálogo web",
  routes: catalogWebFeatureRoutes,
};
