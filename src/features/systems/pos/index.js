import { posFeatureRoutes, posRoutesModel, getPosRoute } from "../../pos";

export { posFeatureRoutes, posRoutesModel, getPosRoute };

export const posSystem = {
  key: "pos",
  label: "Sistema POS",
  routes: posFeatureRoutes,
};
